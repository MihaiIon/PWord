define( [
	/*"effect/size",
	"effect/opacity",*/
	"effect/bump"/*,
	"effect/colorBump"*/
], function( size, opacity, bump, colorBump ){
	return {
		size: size,
		opacity: opacity,
		bump: bump,
		colorBump: colorBump
	};
} );